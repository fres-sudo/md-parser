[package]
name = "md-parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "md_parser"
path = "src/lib.rs"

[dependencies]
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
